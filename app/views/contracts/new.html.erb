<%= content_for :body_title do%>
<% city = ' for ' << @city.name if @city %>
  <% company = ' for ' << @company.sei.to_s if @company %>
    <%= 'New Contract' << (city || ' ') << (company || ' ')%>
  <%end%>
  <%= render 'form', contract: @contract %>