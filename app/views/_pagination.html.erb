<% if local_assigns[:position] == :top %>
  <div class="row justify-content-center">
      <%= page_entries_info value %>
  </div>
<% end %>

<div class="row justify-content-center">
  <%= will_paginate value, :previous_label => '<<', :next_label => '>>', inner_window: 3, outer_window: 0 %>
</div>

<% if local_assigns[:position] == :bottom %>
  <div class="row justify-content-center">
      <%= page_entries_info value %>
  </div>
<% end %>
