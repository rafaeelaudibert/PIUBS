<%= render 'layouts/messages' %>
<div class="auth">
  <div class="auth-container">
    <div class="card">
      <header class="auth-header">
        <h1 class="auth-title">
          <%= image_tag("icons/logo.png", :alt => "PIUBS") %>
          PIUBS
        </h1>
      </header>
      <div class="auth-content">
        <p class="text-center">LOGUE PARA CONTINUAR</p>
        <%= form_for(resource, :as => resource_name, :url => session_path(resource_name), :html => { :role => 'form'}) do |f| %>
        <%= devise_error_messages! %>
        <div class="form-group">
          <%= f.label :email %>
          <%= f.email_field :email, :autofocus => true, placeholder: 'Insira seu email', class: 'form-control underlined' %>
        </div>

        <div class="form-group">
          <%= f.label :password, 'Senha' %>
          <%= f.password_field :password, placeholder: 'Insira sua senha' , class: 'form-control underlined' %>
        </div>

        <div class="form-group">
          <label class="customcheck" style="font-size: 0.8rem">Lembrar de mim
            <input type="checkbox" id="remember">
            <span class="checkmark"></span>
          </label>
          <%= link_to "Esqueceu sua senha?", new_password_path(resource_name), class: 'forgot-btn pull-right' %>
        </div>

        <div class="form-group">
          <%= f.submit 'Login', :class => 'btn btn-block btn-primary' %>
        </div>

        <div class="form-group">
          <p class="text-muted text-center">
            Portal Piubs - Apoio a Empresas & Solução de Controvérsias
          </p>
        </div>
      <% end %>
    </div>
  </div>
  <div class="text-center">
    <%= link_to(root_path, class: 'btn btn-secondary btn-sm') do%>
      <i class="fa fa-arrow-left"></i>
        Voltar
    <%end%>
    <p class="ml-2 btn btn-secondary btn-sm">
      <i class="fa fa-plus-square"></i>
      Copyright &copy; PIUBS AE - Version 0.4.0
    </p>
  </div>
</div>
</div>
